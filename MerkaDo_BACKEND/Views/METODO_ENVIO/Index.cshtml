@model IEnumerable<MerkaDo_BACKEND.Models.METODO_ENVIO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreMetodoEnvio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcionMetodoEnvio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreMetodoEnvio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcionMetodoEnvio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.metodoEnvioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.metodoEnvioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.metodoEnvioId })
        </td>
    </tr>
}

</table>
