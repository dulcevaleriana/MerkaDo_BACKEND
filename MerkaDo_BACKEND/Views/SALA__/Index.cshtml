@model IEnumerable<MerkaDo_BACKEND.Models.SALA__>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_SALA__.nombreSala)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_SALA__.nombreSala)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.salaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.salaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.salaId })
        </td>
    </tr>
}

</table>
