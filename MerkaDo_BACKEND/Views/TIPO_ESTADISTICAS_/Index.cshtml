@model IEnumerable<MerkaDo_BACKEND.Models.TIPO_ESTADISTICAS_>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreTipoEstadisticas)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreTipoEstadisticas)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tipoEstadisticasId }) |
            @Html.ActionLink("Details", "Details", new { id=item.tipoEstadisticasId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tipoEstadisticasId })
        </td>
    </tr>
}

</table>
