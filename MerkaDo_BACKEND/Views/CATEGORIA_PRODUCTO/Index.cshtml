@model IEnumerable<MerkaDo_BACKEND.Models.CATEGORIA_PRODUCTO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombrecategoriaProducto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombrecategoriaProducto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.categoriaProductoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.categoriaProductoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.categoriaProductoId })
        </td>
    </tr>
}

</table>
