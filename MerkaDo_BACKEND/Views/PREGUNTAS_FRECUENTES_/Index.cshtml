@model IEnumerable<MerkaDo_BACKEND.Models.PREGUNTAS_FRECUENTES_>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.preguntaPreguntasFrecuentes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.respuestaPreguntasFrecuentes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.preguntaPreguntasFrecuentes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.respuestaPreguntasFrecuentes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.preguntasFrecuentesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.preguntasFrecuentesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.preguntasFrecuentesId })
        </td>
    </tr>
}

</table>
