@model IEnumerable<MerkaDo_BACKEND.Models.COMPROBANTE_FISCAL>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.rnc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CLIENTES__.nombreCliente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rnc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CLIENTES__.nombreCliente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.comprobanteFiscalId }) |
            @Html.ActionLink("Details", "Details", new { id=item.comprobanteFiscalId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.comprobanteFiscalId })
        </td>
    </tr>
}

</table>
