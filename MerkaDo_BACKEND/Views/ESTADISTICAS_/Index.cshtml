@model IEnumerable<MerkaDo_BACKEND.Models.ESTADISTICAS_>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fechaCreacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.documentoEstadisticas)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_ESTADISTICAS_.nombreTipoEstadisticas)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fechaCreacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.documentoEstadisticas)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_ESTADISTICAS_.nombreTipoEstadisticas)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.estadisticasId }) |
            @Html.ActionLink("Details", "Details", new { id=item.estadisticasId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.estadisticasId })
        </td>
    </tr>
}

</table>
